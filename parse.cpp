#include <cstdio>
#include <cstring>
#include <string>
#include <vector>

#define REP(a,b) for (int a = 0; a < b; a++)

using namespace std;

char s1[100], s2[100];
int t;

int main(){
	while (scanf("(%d, \'%[^\']\', \'%[^\']\'\n", &t, s1, s2) != EOF){
		string st1 = s1;
		string st2 = s2;

		printf("%s\n", s1);
	}
}